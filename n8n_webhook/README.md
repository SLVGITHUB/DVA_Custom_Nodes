# n8n Webhook Receiver для ComfyUI

Простая нода для приема вебхуков от n8n.

## Установка

1. Скопируйте папку `n8n_webhook` в `ComfyUI/custom_nodes/`
2. Установите зависимости:
   ```bash
   pip install flask